{
  "name": "run-map",
  "version": "1.0.0",
  "private": true,
  "description": "Run Map — mappa e PB delle gare",
  "type": "module",
  "scripts": {
    "start": "node scripts/server.mjs",
    "serve": "npx serve ."
  }
}
